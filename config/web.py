#Web interface variables (enable by adding 'web' to script list in /config/base
# .py)
web_interface_port1 = 8008
web_interface_port2 = 5470

#Passwords for connecting to web interface, can be added more
passwords = {
    'admin': "REPLACEME"
}
#Authorization key. Consider change it after download.
#Needed to authorize each websocket connection
auth_key = "f7sd6f78sd6f78sd6"